[package]
name = "waves-protobuf-schemas"
version = "1.4.3"
edition = "2018"

[dependencies]
tonic = "0.8"
prost = { version = "0.11", features = ["no-recursion-limit"] }

[build-dependencies]
tonic-build = "0.8"
